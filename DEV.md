# Development & Release

## Versioning

Versioning and release is automated via [semantic-release](https://www.npmjs.com/package/semantic-release)

## Commands

Publish dry run

```shell
npm publish -ws --dry-run
```

Semantic version dry run

```shell
npx semantic-release --dry-run --no-ci
```
